[package]
name = "rust-sitter-benchmarks"
version = "0.1.0"
edition = "2024"

[[bench]]
name = "parse_bench"
harness = false

[[bench]]
name = "glr_performance"
harness = false

[dependencies]
rust-sitter = { path = "../runtime" }
rust-sitter-example = { path = "../example" }
rust-sitter-python = { path = "../grammars/python" }
tree-sitter = "0.20"
criterion = "0.5"

[dev-dependencies]