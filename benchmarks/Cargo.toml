[package]
name = "rust-sitter-benchmarks"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "parse_bench"
harness = false

[dependencies]
rust-sitter = { path = "../runtime" }
tree-sitter = "0.20"
criterion = "0.5"

[dev-dependencies]