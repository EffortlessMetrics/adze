[package]
name = "rust-sitter-python"
version = "0.1.0"
edition = "2021"

[dependencies]
rust-sitter = { path = "../../runtime" }

[build-dependencies]
rust-sitter-tool = { path = "../../tool" }

[dev-dependencies]
insta = "1.0"