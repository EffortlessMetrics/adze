[package]
name = "adze-python-simple"
version = "0.1.0"
edition = "2024"

[features]
default = ["pure-rust"]
pure-rust = ["adze/pure-rust"]

[dependencies]
adze = { path = "../../runtime", default-features = false }

[build-dependencies]
adze-tool = { path = "../../tool" }