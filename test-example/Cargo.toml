[package]
name = "test-example"
version = "0.1.0"
edition = "2024"

[dependencies]
rust-sitter = { path = "../runtime", features = ["tree-sitter-c2rust"] }

[build-dependencies]
rust-sitter-tool = { path = "../tool" }

[workspace]