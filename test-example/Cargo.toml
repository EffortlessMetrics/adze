[package]
name = "test-example"
version = "0.1.0"
edition = "2024"

[dependencies]
adze = { path = "../runtime", features = ["tree-sitter-c2rust"] }

[build-dependencies]
adze-tool = { path = "../tool" }

[workspace]