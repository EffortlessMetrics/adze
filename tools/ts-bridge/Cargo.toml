[package]
name = "ts-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
libloading = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
anyhow = "1"
rand = "0.8"

[build-dependencies]
cc = "1.0"

[dev-dependencies]
# tree-sitter-json = "0.24"  # Commented out for now, will be used when proper tree-sitter is available

[features]
default = []