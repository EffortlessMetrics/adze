[package]
name = "adze-syn-type-utils-core"
description = "Core syn Type utilities for extraction/filtering/wrapping"
version = "0.8.0-dev"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
syn = { version = "2", features = ["full", "extra-traits"] }
quote = "1"
