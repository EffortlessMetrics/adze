# adze-parser-contract

Parser trait contracts for Adze.

## Overview

This microcrate defines the trait contracts that all Adze parser implementations must
satisfy. It provides the `ParserContract` trait and associated types used for dependency
inversion between the runtime and parser backends.

## License

MIT OR Apache-2.0
