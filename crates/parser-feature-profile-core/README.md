# adze-parser-feature-profile-core

Parser feature-profile contracts extracted from `adze-feature-policy-core`.

This crate owns `ParserFeatureProfile` and backend resolution semantics so other
policy/governance crates can depend on profile logic directly without pulling
additional facade concerns.
