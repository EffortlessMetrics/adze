# adze-runtime-profile-core

Core parser feature-profile helpers shared by runtime governance crates.

This crate centralizes:

- compile-time runtime profile detection,
- runtime2 GLR-toggle profile construction,
- backend resolution from explicit profiles.
