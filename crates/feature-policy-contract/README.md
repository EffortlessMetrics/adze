# adze-feature-policy-contract

Shared parser backend selection and feature-flag profile contracts.

## Overview

Defines trait contracts for parser feature policies — which backend to use (Tree-sitter
vs pure-Rust GLR), which features are enabled, and how to query the active profile at
runtime.

## License

MIT OR Apache-2.0
