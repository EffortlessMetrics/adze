[package]
name = "rust-sitter-macro"
description = "Procedural macros for Rust Sitter"
readme = "../README.md"
repository = "https://github.com/EffortlessMetrics/rust-sitter"
version = "0.8.0-dev"
authors = ["Shadaj Laddad <shadaj@users.noreply.github.com>"]
edition = "2024"
license = "MIT OR Apache-2.0"
keywords = ["parsing", "codegen"]
categories = ["development-tools"]

[lib]
proc-macro = true
path = "src/lib.rs"

[features]
pure-rust = []
strict_docs = [] # Enable strict documentation requirements

[dependencies]
syn = { version = "2", features = [ "full", "extra-traits" ] }
quote = "1"
proc-macro2 = "1"
rust-sitter-common = { version = "0.8.0-dev", path = "../common" }

[dev-dependencies]
insta = "1.39"
tempfile = "3"
trybuild = "1.0"
rust-sitter = { version = "0.8.0-dev", path = "../runtime" }
